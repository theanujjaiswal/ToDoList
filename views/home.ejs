<html>
    <head>
        <title> <%= title%> </title>
        <link href="https://fonts.googleapis.com/css?family=Lato:100"     rel="stylesheet">
        <link rel="stylesheet" href="/TodoList/assets/css/home.css">
  </head>
<body>
    <div class="container">
        <h2> A Simple ToDo List App </h2>
        <form action ="/addtask" method="POST">
        <input type="text" name="newtask" placeholder="Description">     
        <select name="Choose a Category" id="choose" placeholder="Choose a Category">
            <option value="personal">Personal</option>
            <option value="work">Work</option>
            <option value="school">School</option>
            <option value="other">Other</option>
        </select>   
        <button id='button-task'> Add Task </button>
        </form>
        
        <h2> Added Task </h2>
        <div id="contact-list-container">
            <ul>
                <%for(let i of task_list) {%>
                <li class="contact">
                    <div class="details">
                    <p><%= i.newtask%></p>  
                    <p><%= i.category%></p>
                    </div>
                    <div id="delete-button">
                        <a href="/delete-Task/?id=<%= i._id%>">
                        <button>Delete Task</button>
                        </a>
                    </div>
                </li>
                <%}%>
            </ul>
        </div>
            
        
        <h2> Completed task </h2>
    </div>
    <style>
    .container{
    margin: 0px;
    display: block;
    text-align: center;
    }
    #button-task {
    width: 150px;
    height: 40px;
    font-family: monospace;
    font-size: 1.5rem;
    cursor: pointer;
    color: rgb(3, 58, 7);
    }
    form {
    display: inline-flex;
    justify-content: space-around;
    width: 50%;
    padding: 50px 0;
    background-image: linear-gradient(-90deg, floralwhite, azure);
    }
    #delete-button{
    width: 150px;
    height: 40px;
    font-family: monospace;
    font-size: 1.5rem;
    cursor: pointer;
    color: rgb(255, 14, 14);
    }
    </style>
</body>
</html>